@page
@model JurayKV.UI.Areas.KvMain.Pages.IUserMessages.IndexModel
@{
}
<div class="card">
    <div class="card-header">
        <h3 class="card-title">USER MESSAGES</h3>


        <a asp-page="./Add" class="btn bg-gradient-warning btn-sm" style="float:right;">Add New</a>

    </div>
    <!-- /.card-header -->
    <div class="card-body">
        <div class="table-responsive">
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>
                        <th>S/N</th> 
                        <th>
                            @Html.DisplayNameFor(model => model.Messages[0].Title)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Messages[0].Date)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Messages[0].Disable)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Messages[0].All)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Messages[0].Read)
                        </th>
                        <th></th>
                    </tr>
                </thead>

                @{
                    int sn = 0;
                }
                <tbody>
                    @foreach (var item in Model.Messages)
                    {
                        <tr>

                            <td>
                                @{
                                    sn++;
                                }
                                @sn
                            </td>

                            <td>
                                @Html.DisplayFor(modelItem => item.Title)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Disable)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.All)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.Read)
                            </td>
                            <td>
                                <div class="btn-group">
                                    <button type="button" class="btn btn-success">Action</button>

                                    <div class="btn-group">
                                        <button type="button" class="btn btn-success dropdown-toggle dropdown-icon" data-toggle="dropdown" aria-expanded="false">
                                        </button>
                                        <div class="dropdown-menu" style="">

                                            <a class="dropdown-item" asp-page="./Update" asp-route-id="@item.Id">Edit</a>

                                        </div>
                                    </div>
                                </div>

                            </td>

                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>